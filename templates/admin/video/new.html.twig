{# templates/admin/category/new.html.twig #}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% extends 'base.html.twig' %}

{% block title %}Titre{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <div class="page-title d-flex flex-row justify-content-between">
                <h1 class="">Nouvelle vidéo</h1>
                <div class="">
                    <a href="{{ path('admin_videos_list') }}" class="btn btn-sm btn-default">Retour</a>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            {{ form(form) }}
        </div>
    </div>
</div>
{% endblock %}